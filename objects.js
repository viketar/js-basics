let person = {
    name: 'Viktor',
    age: 30
};

console.log(person);

// Change the persons name with dot notation
person.name = 'John';

//... with Bracket notation
person['name'] = 'Mary';

console.log(person.name);
