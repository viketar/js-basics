    //This is my first js code!
    // console.log('Hello world...!')'

    let name = 'Viktor'; // String
    console.log(name);
    let age = 30; // Number
    let isApproved = true; // Boolean (true; or false;)
    // let firstName; // undefined variable
    let selectedColour = null; // 


    // Rules for naming variables: Cannot be a reserved keyword (i.e let, var, if and so on)
    // Should be meaningful names on variables (avoid x, a1 and so on)
    // Cannot start with a number
    // Cannot contain a space or hyphen (-)
    // They are case sensitive

    let firstName = 'Viktor'
    let lastName = 'Ankarberg';
    console.log(firstName, lastName);


    // Constant (the value of a constant cannot change)
    // If you need to reassign a variable - use let. If not, const.
    // The code beloew will generate an error:
    const interestRate = 0.3;
    interestRate = 1;
    console.log(interestRate);
    